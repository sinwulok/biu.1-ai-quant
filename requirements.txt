# Core dependencies
yfinance>=0.2.18
pandas>=1.5.0
numpy>=1.23.0

# API and web framework
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Data processing and analysis
scipy>=1.11.0

# Broker integrations (optional — enable in config.py)
# Install only what you need:
ccxt>=4.0.0              # Crypto exchanges (CCXT data + broker)
# ib_insync>=0.9.86      # Interactive Brokers (requires TWS/IB Gateway)
# futu-openapi>=9.0.0    # Futu OpenD (requires OpenD daemon)

# Testing and development
pytest>=7.4.0
pytest-cov>=4.1.0
